import React from 'react';
import { SearchBar } from '../search/SearchBar';

export default function HomePage(props) {


    return (
        <main>
            <h1>[Title] or Logo</h1>
            <SearchBar/>
        </main>
    );
}