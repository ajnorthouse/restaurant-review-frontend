import React from 'react';
import { SearchBar } from './SearchBar';

export default function Home(props) {


    return (
        <main>
            <h1>[Title] or Logo</h1>
            <SearchBar/>
        </main>
    );
}